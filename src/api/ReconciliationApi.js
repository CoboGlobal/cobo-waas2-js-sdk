/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import ErrorResponse from '../model/ErrorResponse';
import GetReconciliationLedger200Response from '../model/GetReconciliationLedger200Response';
import ListReconciliationStatements200Response from '../model/ListReconciliationStatements200Response';

/**
* Reconciliation service.
* @module api/ReconciliationApi
*/
export default class ReconciliationApi {

    /**
    * Constructs a new ReconciliationApi. 
    * @alias module:api/ReconciliationApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }



    /**
     * Get reconciliation ledger
     * This operation retrieves the post-transaction balance (running balance) for the specified transactions, used for stablecoin deposit and withdrawal reconciliation.  You need to provide the transaction IDs in `transaction_ids`. Each returned entry includes the signed amount and the resulting balance of the address after the transaction, expressed in the token's main unit.  <Note>This operation is available to selected customers only. To request access, please contact Cobo.</Note>  <Note>This operation is applicable to MPC Wallets and Custodial Web3 Wallets only, and covers stablecoins only. To ensure accurate reconciliation results, do not use the contract call and message signing features. Currently, only stablecoins on the Ethereum and TRON chains are supported.</Note> 
     * @param {String} transaction_ids A list of transaction IDs, separated by comma. You can specify 1 to 100 transaction IDs.
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/GetReconciliationLedger200Response} and HTTP response
     */
    getReconciliationLedgerWithHttpInfo(transaction_ids) {
      let postBody = null;
      if (postBody && postBody.toJSON) {
          postBody = postBody.toJSON()
      }
      // verify the required parameter 'transaction_ids' is set
      if (transaction_ids === undefined || transaction_ids === null) {
        throw new Error("Missing the required parameter 'transaction_ids' when calling getReconciliationLedger");
      }

      let pathParams = {
      };
      let queryParams = {
        'transaction_ids': transaction_ids
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['OAuth2', 'CoboAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = GetReconciliationLedger200Response;
      return this.apiClient.callApi(
        '/recon/ledger', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Get reconciliation ledger
     * This operation retrieves the post-transaction balance (running balance) for the specified transactions, used for stablecoin deposit and withdrawal reconciliation.  You need to provide the transaction IDs in `transaction_ids`. Each returned entry includes the signed amount and the resulting balance of the address after the transaction, expressed in the token's main unit.  <Note>This operation is available to selected customers only. To request access, please contact Cobo.</Note>  <Note>This operation is applicable to MPC Wallets and Custodial Web3 Wallets only, and covers stablecoins only. To ensure accurate reconciliation results, do not use the contract call and message signing features. Currently, only stablecoins on the Ethereum and TRON chains are supported.</Note> 
     * @param {String} transaction_ids A list of transaction IDs, separated by comma. You can specify 1 to 100 transaction IDs.
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with data of type {@link module:model/GetReconciliationLedger200Response}
     */
    getReconciliationLedger(transaction_ids) {
      return this.getReconciliationLedgerWithHttpInfo(transaction_ids)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * List reconciliation daily statements
     * This operation retrieves daily reconciliation statements within a date range. Each statement contains the opening balance, total deposits, total withdrawals, and closing balance for a business date, wallet, and token.  You need to specify the date range with `start_date` and `end_date`. You can filter the results by wallets and tokens, and paginate the query results. All amounts are expressed in the token's main unit.  <Note>This operation is available to selected customers only. To request access, please contact Cobo.</Note>  <Note>This operation is applicable to MPC Wallets and Custodial Web3 Wallets only, and covers stablecoins only. To ensure accurate reconciliation results, do not use the contract call and message signing features. Currently, only stablecoins on the Ethereum and TRON chains are supported.</Note> 
     * @param {Date} start_date The start date of the reconciliation period (inclusive), in YYYY-MM-DD format (UTC). The range between `start_date` and `end_date` must not exceed 366 days.
     * @param {Date} end_date The end date of the reconciliation period (inclusive), in YYYY-MM-DD format (UTC). The range between `start_date` and `end_date` must not exceed 366 days.
     * @param {Object} opts Optional parameters
     * @param {String} [wallet_ids] A list of wallet IDs, separated by comma.
     * @param {String} [token_ids] A list of token IDs, separated by comma. The token ID is the unique identifier of a token. You can retrieve the IDs of all the tokens you can use by calling [List enabled tokens](https://www.cobo.com/developers/v2/api-references/wallets/list-enabled-tokens).
     * @param {Number} [limit = 10)] The maximum number of objects to return. For most operations, the value range is [1, 50].
     * @param {String} [before] A cursor indicating the position before the current page. This value is generated by Cobo and returned in the response. If you are paginating forward from the beginning, you do not need to provide it on the first request. When paginating backward (to the previous page), you should pass the before value returned from the last response. 
     * @param {String} [after] A cursor indicating the position after the current page. This value is generated by Cobo and returned in the response. You do not need to provide it on the first request. When paginating forward (to the next page), you should pass the after value returned from the last response. 
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/ListReconciliationStatements200Response} and HTTP response
     */
    listReconciliationStatementsWithHttpInfo(start_date, end_date, opts) {
      opts = opts || {};
      let postBody = null;
      if (postBody && postBody.toJSON) {
          postBody = postBody.toJSON()
      }
      // verify the required parameter 'start_date' is set
      if (start_date === undefined || start_date === null) {
        throw new Error("Missing the required parameter 'start_date' when calling listReconciliationStatements");
      }
      // verify the required parameter 'end_date' is set
      if (end_date === undefined || end_date === null) {
        throw new Error("Missing the required parameter 'end_date' when calling listReconciliationStatements");
      }

      let pathParams = {
      };
      let queryParams = {
        'start_date': start_date,
        'end_date': end_date,
        'wallet_ids': opts['wallet_ids'],
        'token_ids': opts['token_ids'],
        'limit': opts['limit'],
        'before': opts['before'],
        'after': opts['after']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['OAuth2', 'CoboAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = ListReconciliationStatements200Response;
      return this.apiClient.callApi(
        '/recon/statements', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * List reconciliation daily statements
     * This operation retrieves daily reconciliation statements within a date range. Each statement contains the opening balance, total deposits, total withdrawals, and closing balance for a business date, wallet, and token.  You need to specify the date range with `start_date` and `end_date`. You can filter the results by wallets and tokens, and paginate the query results. All amounts are expressed in the token's main unit.  <Note>This operation is available to selected customers only. To request access, please contact Cobo.</Note>  <Note>This operation is applicable to MPC Wallets and Custodial Web3 Wallets only, and covers stablecoins only. To ensure accurate reconciliation results, do not use the contract call and message signing features. Currently, only stablecoins on the Ethereum and TRON chains are supported.</Note> 
     * @param {Date} start_date The start date of the reconciliation period (inclusive), in YYYY-MM-DD format (UTC). The range between `start_date` and `end_date` must not exceed 366 days.
     * @param {Date} end_date The end date of the reconciliation period (inclusive), in YYYY-MM-DD format (UTC). The range between `start_date` and `end_date` must not exceed 366 days.
     * @param {Object} opts Optional parameters
     * @param {String} opts.wallet_ids A list of wallet IDs, separated by comma.
     * @param {String} opts.token_ids A list of token IDs, separated by comma. The token ID is the unique identifier of a token. You can retrieve the IDs of all the tokens you can use by calling [List enabled tokens](https://www.cobo.com/developers/v2/api-references/wallets/list-enabled-tokens).
     * @param {Number} opts.limit The maximum number of objects to return. For most operations, the value range is [1, 50]. (default to 10)
     * @param {String} opts.before A cursor indicating the position before the current page. This value is generated by Cobo and returned in the response. If you are paginating forward from the beginning, you do not need to provide it on the first request. When paginating backward (to the previous page), you should pass the before value returned from the last response. 
     * @param {String} opts.after A cursor indicating the position after the current page. This value is generated by Cobo and returned in the response. You do not need to provide it on the first request. When paginating forward (to the next page), you should pass the after value returned from the last response. 
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with data of type {@link module:model/ListReconciliationStatements200Response}
     */
    listReconciliationStatements(start_date, end_date, opts) {
      return this.listReconciliationStatementsWithHttpInfo(start_date, end_date, opts)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


}
