/**
 * Cobo Wallet as a Service 2.0
 * Cobo WaaS 2.0 enables you to programmatically access Cobo's full suite of crypto wallet technologies with powerful and flexible access controls.  # Wallet technologies - Custodial Wallet - MPC Wallet - Smart Contract Wallet (Based on Safe{Wallet}) - Exchange Wallet  # Risk Control technologies - Workflow - Access Control List (ACL)  # Risk Control targets - Wallet Management   - User/team and their permission management   - Risk control configurations, e.g. whitelist, blacklist, rate-limiting etc. - Blockchain Interaction   - Crypto transfer   - Smart Contract Invocation  # Important HTTPS only. RESTful, resource oriented  # Get Started Set up your APIs or get authorization  # Authentication and Authorization CoboAuth  # Request and Response application/json  # Error Handling  ### Common error codes | Error Code | Description | | -- | -- |  ### API-specific error codes For error codes that are dedicated to a specific API, see the Error codes section in each API specification, for example, /v3/wallets.  # Rate and Usage Limiting  # Idempotent Request  # Pagination # Support [Developer Hub](https://cobo.com/developers) 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AddressEncoding from './AddressEncoding';

/**
 * The AddWalletAddressRequest model module.
 * @module model/AddWalletAddressRequest
 * @version 0.1.0
 */
class AddWalletAddressRequest {
    /**
     * Constructs a new <code>AddWalletAddressRequest</code>.
     * @alias module:model/AddWalletAddressRequest
     * @param tokenId {String} ID of the token. Unique in all chains scope.
     * @param count {Number} The number of addresses that need to be created
     */
    constructor(tokenId, count) { 
        
        AddWalletAddressRequest.initialize(this, tokenId, count);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, tokenId, count) { 
        obj['token_id'] = tokenId;
        obj['count'] = count || 1;
    }

    /**
     * Constructs a <code>AddWalletAddressRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AddWalletAddressRequest} obj Optional instance to populate.
     * @return {module:model/AddWalletAddressRequest} The populated <code>AddWalletAddressRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AddWalletAddressRequest();

            if (data.hasOwnProperty('token_id')) {
                obj['token_id'] = ApiClient.convertToType(data['token_id'], 'String');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('encoding')) {
                obj['encoding'] = AddressEncoding.constructFromObject(data['encoding']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AddWalletAddressRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AddWalletAddressRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AddWalletAddressRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['token_id'] && !(typeof data['token_id'] === 'string' || data['token_id'] instanceof String)) {
            throw new Error("Expected the field `token_id` to be a primitive type in the JSON string but got " + data['token_id']);
        }

        return true;
    }


}

AddWalletAddressRequest.RequiredProperties = ["token_id", "count"];

/**
 * ID of the token. Unique in all chains scope.
 * @member {String} token_id
 */
AddWalletAddressRequest.prototype['token_id'] = undefined;

/**
 * The number of addresses that need to be created
 * @member {Number} count
 * @default 1
 */
AddWalletAddressRequest.prototype['count'] = 1;

/**
 * @member {module:model/AddressEncoding} encoding
 */
AddWalletAddressRequest.prototype['encoding'] = undefined;






export default AddWalletAddressRequest;

