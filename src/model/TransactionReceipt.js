/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TransactionReceiptLog from './TransactionReceiptLog';

/**
 * The TransactionReceipt model module.
 * @module model/TransactionReceipt
 */
class TransactionReceipt {
    /**
     * Constructs a new <code>TransactionReceipt</code>.
     * The information about a transaction receipt, which records the execution result of a transaction that has been included in a block.  All the numeric properties are returned as decimal values instead of the hexadecimal values used by the JSON-RPC interfaces of the chains. The properties that record on-chain amounts are returned as strings to avoid any precision loss. 
     * @alias module:model/TransactionReceipt
     * @param chain_id {String} The chain ID, which is the unique identifier of a blockchain. You can retrieve the IDs of all the chains you can use by calling [List enabled chains](https://www.cobo.com/developers/v2/api-references/wallets/list-enabled-chains).
     * @param transaction_hash {String} The transaction hash, returned with the `0x` prefix.
     * @param status {Number} The execution result of the transaction. - `1`: The transaction was executed successfully. - `0`: The transaction failed, for example, because it was reverted by the contract or ran out of gas. 
     * @param block_number {Number} The number of the block that contains the transaction.
     * @param block_hash {String} The hash of the block that contains the transaction.
     * @param transaction_index {Number} The index position of the transaction within the block.
     * @param from_address {String} The address that sent the transaction.
     * @param gas_used {String} The number of gas units consumed by the transaction.
     * @param logs {Array.<module:model/TransactionReceiptLog>} The event logs emitted during the execution of the transaction, in the order in which they were emitted. The array is empty if the transaction emitted no logs. A maximum of 1,000 logs are returned. If the transaction emitted more logs, only the first 1,000 are returned and `logs_truncated` is `true`.
     * @param logs_truncated {Boolean} Whether the event logs returned in `logs` were truncated. - `true`: The transaction emitted more than 1,000 logs and only the first 1,000 are returned. - `false`: All the logs emitted by the transaction are returned. 
     */
    constructor(chain_id, transaction_hash, status, block_number, block_hash, transaction_index, from_address, gas_used, logs, logs_truncated) { 
        
        TransactionReceipt.initialize(this, chain_id, transaction_hash, status, block_number, block_hash, transaction_index, from_address, gas_used, logs, logs_truncated);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, chain_id, transaction_hash, status, block_number, block_hash, transaction_index, from_address, gas_used, logs, logs_truncated) { 
        obj['chain_id'] = chain_id;
        obj['transaction_hash'] = transaction_hash;
        obj['status'] = status;
        obj['block_number'] = block_number;
        obj['block_hash'] = block_hash;
        obj['transaction_index'] = transaction_index;
        obj['from_address'] = from_address;
        obj['gas_used'] = gas_used;
        obj['logs'] = logs;
        obj['logs_truncated'] = logs_truncated;
    }

    /**
     * Constructs a <code>TransactionReceipt</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TransactionReceipt} obj Optional instance to populate.
     * @return {module:model/TransactionReceipt} The populated <code>TransactionReceipt</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TransactionReceipt();

            if (data.hasOwnProperty('chain_id')) {
                obj['chain_id'] = ApiClient.convertToType(data['chain_id'], 'String');
            }
            if (data.hasOwnProperty('transaction_hash')) {
                obj['transaction_hash'] = ApiClient.convertToType(data['transaction_hash'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'Number');
            }
            if (data.hasOwnProperty('block_number')) {
                obj['block_number'] = ApiClient.convertToType(data['block_number'], 'Number');
            }
            if (data.hasOwnProperty('block_hash')) {
                obj['block_hash'] = ApiClient.convertToType(data['block_hash'], 'String');
            }
            if (data.hasOwnProperty('transaction_index')) {
                obj['transaction_index'] = ApiClient.convertToType(data['transaction_index'], 'Number');
            }
            if (data.hasOwnProperty('from_address')) {
                obj['from_address'] = ApiClient.convertToType(data['from_address'], 'String');
            }
            if (data.hasOwnProperty('to_address')) {
                obj['to_address'] = ApiClient.convertToType(data['to_address'], 'String');
            }
            if (data.hasOwnProperty('contract_address')) {
                obj['contract_address'] = ApiClient.convertToType(data['contract_address'], 'String');
            }
            if (data.hasOwnProperty('gas_used')) {
                obj['gas_used'] = ApiClient.convertToType(data['gas_used'], 'String');
            }
            if (data.hasOwnProperty('cumulative_gas_used')) {
                obj['cumulative_gas_used'] = ApiClient.convertToType(data['cumulative_gas_used'], 'String');
            }
            if (data.hasOwnProperty('effective_gas_price')) {
                obj['effective_gas_price'] = ApiClient.convertToType(data['effective_gas_price'], 'String');
            }
            if (data.hasOwnProperty('evm_transaction_type')) {
                obj['evm_transaction_type'] = ApiClient.convertToType(data['evm_transaction_type'], 'Number');
            }
            if (data.hasOwnProperty('logs_bloom')) {
                obj['logs_bloom'] = ApiClient.convertToType(data['logs_bloom'], 'String');
            }
            if (data.hasOwnProperty('logs')) {
                obj['logs'] = ApiClient.convertToType(data['logs'], [TransactionReceiptLog]);
            }
            if (data.hasOwnProperty('logs_truncated')) {
                obj['logs_truncated'] = ApiClient.convertToType(data['logs_truncated'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TransactionReceipt</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TransactionReceipt</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TransactionReceipt.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['chain_id'] && !(typeof data['chain_id'] === 'string' || data['chain_id'] instanceof String)) {
            throw new Error("Expected the field `chain_id` to be a primitive type in the JSON string but got " + data['chain_id']);
        }
        // ensure the json data is a string
        if (data['transaction_hash'] && !(typeof data['transaction_hash'] === 'string' || data['transaction_hash'] instanceof String)) {
            throw new Error("Expected the field `transaction_hash` to be a primitive type in the JSON string but got " + data['transaction_hash']);
        }
        // ensure the json data is a string
        if (data['block_hash'] && !(typeof data['block_hash'] === 'string' || data['block_hash'] instanceof String)) {
            throw new Error("Expected the field `block_hash` to be a primitive type in the JSON string but got " + data['block_hash']);
        }
        // ensure the json data is a string
        if (data['from_address'] && !(typeof data['from_address'] === 'string' || data['from_address'] instanceof String)) {
            throw new Error("Expected the field `from_address` to be a primitive type in the JSON string but got " + data['from_address']);
        }
        // ensure the json data is a string
        if (data['to_address'] && !(typeof data['to_address'] === 'string' || data['to_address'] instanceof String)) {
            throw new Error("Expected the field `to_address` to be a primitive type in the JSON string but got " + data['to_address']);
        }
        // ensure the json data is a string
        if (data['contract_address'] && !(typeof data['contract_address'] === 'string' || data['contract_address'] instanceof String)) {
            throw new Error("Expected the field `contract_address` to be a primitive type in the JSON string but got " + data['contract_address']);
        }
        // ensure the json data is a string
        if (data['gas_used'] && !(typeof data['gas_used'] === 'string' || data['gas_used'] instanceof String)) {
            throw new Error("Expected the field `gas_used` to be a primitive type in the JSON string but got " + data['gas_used']);
        }
        // ensure the json data is a string
        if (data['cumulative_gas_used'] && !(typeof data['cumulative_gas_used'] === 'string' || data['cumulative_gas_used'] instanceof String)) {
            throw new Error("Expected the field `cumulative_gas_used` to be a primitive type in the JSON string but got " + data['cumulative_gas_used']);
        }
        // ensure the json data is a string
        if (data['effective_gas_price'] && !(typeof data['effective_gas_price'] === 'string' || data['effective_gas_price'] instanceof String)) {
            throw new Error("Expected the field `effective_gas_price` to be a primitive type in the JSON string but got " + data['effective_gas_price']);
        }
        // ensure the json data is a string
        if (data['logs_bloom'] && !(typeof data['logs_bloom'] === 'string' || data['logs_bloom'] instanceof String)) {
            throw new Error("Expected the field `logs_bloom` to be a primitive type in the JSON string but got " + data['logs_bloom']);
        }
        if (data['logs']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['logs'])) {
                throw new Error("Expected the field `logs` to be an array in the JSON data but got " + data['logs']);
            }
            // validate the optional field `logs` (array)
            for (const item of data['logs']) {
                TransactionReceiptLog.validateJSON(item);
            };
        }

        return true;
    }


}

TransactionReceipt.RequiredProperties = ["chain_id", "transaction_hash", "status", "block_number", "block_hash", "transaction_index", "from_address", "gas_used", "logs", "logs_truncated"];

/**
 * The chain ID, which is the unique identifier of a blockchain. You can retrieve the IDs of all the chains you can use by calling [List enabled chains](https://www.cobo.com/developers/v2/api-references/wallets/list-enabled-chains).
 * @member {String} chain_id
 */
TransactionReceipt.prototype['chain_id'] = undefined;

/**
 * The transaction hash, returned with the `0x` prefix.
 * @member {String} transaction_hash
 */
TransactionReceipt.prototype['transaction_hash'] = undefined;

/**
 * The execution result of the transaction. - `1`: The transaction was executed successfully. - `0`: The transaction failed, for example, because it was reverted by the contract or ran out of gas. 
 * @member {Number} status
 */
TransactionReceipt.prototype['status'] = undefined;

/**
 * The number of the block that contains the transaction.
 * @member {Number} block_number
 */
TransactionReceipt.prototype['block_number'] = undefined;

/**
 * The hash of the block that contains the transaction.
 * @member {String} block_hash
 */
TransactionReceipt.prototype['block_hash'] = undefined;

/**
 * The index position of the transaction within the block.
 * @member {Number} transaction_index
 */
TransactionReceipt.prototype['transaction_index'] = undefined;

/**
 * The address that sent the transaction.
 * @member {String} from_address
 */
TransactionReceipt.prototype['from_address'] = undefined;

/**
 * The address that received the transaction. The value is `null` if the transaction created a contract.
 * @member {String} to_address
 */
TransactionReceipt.prototype['to_address'] = undefined;

/**
 * The address of the contract created by the transaction. The value is `null` if the transaction did not create a contract.
 * @member {String} contract_address
 */
TransactionReceipt.prototype['contract_address'] = undefined;

/**
 * The number of gas units consumed by the transaction.
 * @member {String} gas_used
 */
TransactionReceipt.prototype['gas_used'] = undefined;

/**
 * The total number of gas units consumed by all the transactions up to and including this transaction in the block.
 * @member {String} cumulative_gas_used
 */
TransactionReceipt.prototype['cumulative_gas_used'] = undefined;

/**
 * The gas price actually paid for each gas unit consumed by the transaction, in the smallest unit of the chain's native token. For example, the value is in wei for Ethereum.
 * @member {String} effective_gas_price
 */
TransactionReceipt.prototype['effective_gas_price'] = undefined;

/**
 * The transaction envelope type defined by the chain. This property describes the on-chain transaction format, not the Cobo transaction type returned by [Get transaction information](https://www.cobo.com/developers/v2/api-references/transactions/get-transaction-information). - `0`: A legacy transaction. - `1`: An access list transaction, as defined in EIP-2930. - `2`: A dynamic fee transaction, as defined in EIP-1559. 
 * @member {Number} evm_transaction_type
 */
TransactionReceipt.prototype['evm_transaction_type'] = undefined;

/**
 * The bloom filter of the event logs emitted during the execution of the transaction, which can be used to quickly check whether the transaction emitted a specific log. The value is a 256-byte hexadecimal string.
 * @member {String} logs_bloom
 */
TransactionReceipt.prototype['logs_bloom'] = undefined;

/**
 * The event logs emitted during the execution of the transaction, in the order in which they were emitted. The array is empty if the transaction emitted no logs. A maximum of 1,000 logs are returned. If the transaction emitted more logs, only the first 1,000 are returned and `logs_truncated` is `true`.
 * @member {Array.<module:model/TransactionReceiptLog>} logs
 */
TransactionReceipt.prototype['logs'] = undefined;

/**
 * Whether the event logs returned in `logs` were truncated. - `true`: The transaction emitted more than 1,000 logs and only the first 1,000 are returned. - `false`: All the logs emitted by the transaction are returned. 
 * @member {Boolean} logs_truncated
 */
TransactionReceipt.prototype['logs_truncated'] = undefined;






export default TransactionReceipt;

