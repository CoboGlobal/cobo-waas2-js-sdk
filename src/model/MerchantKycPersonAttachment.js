/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import MerchantKycPersonAttachmentFileType from './MerchantKycPersonAttachmentFileType';

/**
 * The MerchantKycPersonAttachment model module.
 * @module model/MerchantKycPersonAttachment
 */
class MerchantKycPersonAttachment {
    /**
     * Constructs a new <code>MerchantKycPersonAttachment</code>.
     * @alias module:model/MerchantKycPersonAttachment
     * @param file_id {String} The AWS file link of the uploaded file, which you can retrieve by calling [Upload file](https://www.cobo.com/developers/v2/api-references/payment/upload-file). 
     * @param file_type {module:model/MerchantKycPersonAttachmentFileType} 
     */
    constructor(file_id, file_type) { 
        
        MerchantKycPersonAttachment.initialize(this, file_id, file_type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, file_id, file_type) { 
        obj['file_id'] = file_id;
        obj['file_type'] = file_type;
    }

    /**
     * Constructs a <code>MerchantKycPersonAttachment</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MerchantKycPersonAttachment} obj Optional instance to populate.
     * @return {module:model/MerchantKycPersonAttachment} The populated <code>MerchantKycPersonAttachment</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MerchantKycPersonAttachment();

            if (data.hasOwnProperty('file_id')) {
                obj['file_id'] = ApiClient.convertToType(data['file_id'], 'String');
            }
            if (data.hasOwnProperty('file_type')) {
                obj['file_type'] = MerchantKycPersonAttachmentFileType.constructFromObject(data['file_type']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MerchantKycPersonAttachment</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MerchantKycPersonAttachment</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MerchantKycPersonAttachment.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['file_id'] && !(typeof data['file_id'] === 'string' || data['file_id'] instanceof String)) {
            throw new Error("Expected the field `file_id` to be a primitive type in the JSON string but got " + data['file_id']);
        }

        return true;
    }


}

MerchantKycPersonAttachment.RequiredProperties = ["file_id", "file_type"];

/**
 * The AWS file link of the uploaded file, which you can retrieve by calling [Upload file](https://www.cobo.com/developers/v2/api-references/payment/upload-file). 
 * @member {String} file_id
 */
MerchantKycPersonAttachment.prototype['file_id'] = undefined;

/**
 * @member {module:model/MerchantKycPersonAttachmentFileType} file_type
 */
MerchantKycPersonAttachment.prototype['file_type'] = undefined;






export default MerchantKycPersonAttachment;

