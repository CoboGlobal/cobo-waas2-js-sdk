/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ReconStatementStatus from './ReconStatementStatus';

/**
 * The ReconDailyStatement model module.
 * @module model/ReconDailyStatement
 */
class ReconDailyStatement {
    /**
     * Constructs a new <code>ReconDailyStatement</code>.
     * The daily reconciliation statement for a wallet and token on a business date.
     * @alias module:model/ReconDailyStatement
     * @param biz_date {Date} The business date (UTC), in YYYY-MM-DD format.
     * @param token_id {String} The token ID, which is the unique identifier of a token.
     * @param chain_id {String} The chain ID, which is the unique identifier of a blockchain.
     * @param wallet_id {String} The wallet ID.
     * @param opening_balance {String} The opening balance at the start of the business date, expressed in the token's main unit.
     * @param total_deposit {String} The total deposit amount during the business date, expressed in the token's main unit.
     * @param deposit_count {Number} The number of deposits during the business date.
     * @param total_withdrawal {String} The total withdrawal amount during the business date, expressed in the token's main unit.
     * @param withdrawal_count {Number} The number of withdrawals during the business date.
     * @param closing_balance {String} The closing balance at the end of the business date, expressed in the token's main unit.
     * @param status {module:model/ReconStatementStatus} 
     */
    constructor(biz_date, token_id, chain_id, wallet_id, opening_balance, total_deposit, deposit_count, total_withdrawal, withdrawal_count, closing_balance, status) { 
        
        ReconDailyStatement.initialize(this, biz_date, token_id, chain_id, wallet_id, opening_balance, total_deposit, deposit_count, total_withdrawal, withdrawal_count, closing_balance, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, biz_date, token_id, chain_id, wallet_id, opening_balance, total_deposit, deposit_count, total_withdrawal, withdrawal_count, closing_balance, status) { 
        obj['biz_date'] = biz_date;
        obj['token_id'] = token_id;
        obj['chain_id'] = chain_id;
        obj['wallet_id'] = wallet_id;
        obj['opening_balance'] = opening_balance;
        obj['total_deposit'] = total_deposit;
        obj['deposit_count'] = deposit_count;
        obj['total_withdrawal'] = total_withdrawal;
        obj['withdrawal_count'] = withdrawal_count;
        obj['closing_balance'] = closing_balance;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>ReconDailyStatement</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReconDailyStatement} obj Optional instance to populate.
     * @return {module:model/ReconDailyStatement} The populated <code>ReconDailyStatement</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReconDailyStatement();

            if (data.hasOwnProperty('biz_date')) {
                obj['biz_date'] = ApiClient.convertToType(data['biz_date'], 'Date');
            }
            if (data.hasOwnProperty('token_id')) {
                obj['token_id'] = ApiClient.convertToType(data['token_id'], 'String');
            }
            if (data.hasOwnProperty('chain_id')) {
                obj['chain_id'] = ApiClient.convertToType(data['chain_id'], 'String');
            }
            if (data.hasOwnProperty('wallet_id')) {
                obj['wallet_id'] = ApiClient.convertToType(data['wallet_id'], 'String');
            }
            if (data.hasOwnProperty('opening_balance')) {
                obj['opening_balance'] = ApiClient.convertToType(data['opening_balance'], 'String');
            }
            if (data.hasOwnProperty('total_deposit')) {
                obj['total_deposit'] = ApiClient.convertToType(data['total_deposit'], 'String');
            }
            if (data.hasOwnProperty('deposit_count')) {
                obj['deposit_count'] = ApiClient.convertToType(data['deposit_count'], 'Number');
            }
            if (data.hasOwnProperty('total_withdrawal')) {
                obj['total_withdrawal'] = ApiClient.convertToType(data['total_withdrawal'], 'String');
            }
            if (data.hasOwnProperty('withdrawal_count')) {
                obj['withdrawal_count'] = ApiClient.convertToType(data['withdrawal_count'], 'Number');
            }
            if (data.hasOwnProperty('closing_balance')) {
                obj['closing_balance'] = ApiClient.convertToType(data['closing_balance'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ReconStatementStatus.constructFromObject(data['status']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReconDailyStatement</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReconDailyStatement</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ReconDailyStatement.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['token_id'] && !(typeof data['token_id'] === 'string' || data['token_id'] instanceof String)) {
            throw new Error("Expected the field `token_id` to be a primitive type in the JSON string but got " + data['token_id']);
        }
        // ensure the json data is a string
        if (data['chain_id'] && !(typeof data['chain_id'] === 'string' || data['chain_id'] instanceof String)) {
            throw new Error("Expected the field `chain_id` to be a primitive type in the JSON string but got " + data['chain_id']);
        }
        // ensure the json data is a string
        if (data['wallet_id'] && !(typeof data['wallet_id'] === 'string' || data['wallet_id'] instanceof String)) {
            throw new Error("Expected the field `wallet_id` to be a primitive type in the JSON string but got " + data['wallet_id']);
        }
        // ensure the json data is a string
        if (data['opening_balance'] && !(typeof data['opening_balance'] === 'string' || data['opening_balance'] instanceof String)) {
            throw new Error("Expected the field `opening_balance` to be a primitive type in the JSON string but got " + data['opening_balance']);
        }
        // ensure the json data is a string
        if (data['total_deposit'] && !(typeof data['total_deposit'] === 'string' || data['total_deposit'] instanceof String)) {
            throw new Error("Expected the field `total_deposit` to be a primitive type in the JSON string but got " + data['total_deposit']);
        }
        // ensure the json data is a string
        if (data['total_withdrawal'] && !(typeof data['total_withdrawal'] === 'string' || data['total_withdrawal'] instanceof String)) {
            throw new Error("Expected the field `total_withdrawal` to be a primitive type in the JSON string but got " + data['total_withdrawal']);
        }
        // ensure the json data is a string
        if (data['closing_balance'] && !(typeof data['closing_balance'] === 'string' || data['closing_balance'] instanceof String)) {
            throw new Error("Expected the field `closing_balance` to be a primitive type in the JSON string but got " + data['closing_balance']);
        }

        return true;
    }


}

ReconDailyStatement.RequiredProperties = ["biz_date", "token_id", "chain_id", "wallet_id", "opening_balance", "total_deposit", "deposit_count", "total_withdrawal", "withdrawal_count", "closing_balance", "status"];

/**
 * The business date (UTC), in YYYY-MM-DD format.
 * @member {Date} biz_date
 */
ReconDailyStatement.prototype['biz_date'] = undefined;

/**
 * The token ID, which is the unique identifier of a token.
 * @member {String} token_id
 */
ReconDailyStatement.prototype['token_id'] = undefined;

/**
 * The chain ID, which is the unique identifier of a blockchain.
 * @member {String} chain_id
 */
ReconDailyStatement.prototype['chain_id'] = undefined;

/**
 * The wallet ID.
 * @member {String} wallet_id
 */
ReconDailyStatement.prototype['wallet_id'] = undefined;

/**
 * The opening balance at the start of the business date, expressed in the token's main unit.
 * @member {String} opening_balance
 */
ReconDailyStatement.prototype['opening_balance'] = undefined;

/**
 * The total deposit amount during the business date, expressed in the token's main unit.
 * @member {String} total_deposit
 */
ReconDailyStatement.prototype['total_deposit'] = undefined;

/**
 * The number of deposits during the business date.
 * @member {Number} deposit_count
 */
ReconDailyStatement.prototype['deposit_count'] = undefined;

/**
 * The total withdrawal amount during the business date, expressed in the token's main unit.
 * @member {String} total_withdrawal
 */
ReconDailyStatement.prototype['total_withdrawal'] = undefined;

/**
 * The number of withdrawals during the business date.
 * @member {Number} withdrawal_count
 */
ReconDailyStatement.prototype['withdrawal_count'] = undefined;

/**
 * The closing balance at the end of the business date, expressed in the token's main unit.
 * @member {String} closing_balance
 */
ReconDailyStatement.prototype['closing_balance'] = undefined;

/**
 * @member {module:model/ReconStatementStatus} status
 */
ReconDailyStatement.prototype['status'] = undefined;






export default ReconDailyStatement;

