/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TransactionReceiptLog model module.
 * @module model/TransactionReceiptLog
 */
class TransactionReceiptLog {
    /**
     * Constructs a new <code>TransactionReceiptLog</code>.
     * The information about an event log emitted during the execution of a transaction.
     * @alias module:model/TransactionReceiptLog
     * @param log_index {Number} The index position of the log within the block.
     * @param address {String} The address of the contract that emitted the log.
     * @param topics {Array.<String>} The indexed log arguments. The first topic is the hash of the event signature, and the remaining topics are the indexed event parameters, with a maximum of three.
     * @param data {String} The non-indexed log arguments, encoded as a hexadecimal string.
     */
    constructor(log_index, address, topics, data) { 
        
        TransactionReceiptLog.initialize(this, log_index, address, topics, data);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, log_index, address, topics, data) { 
        obj['log_index'] = log_index;
        obj['address'] = address;
        obj['topics'] = topics;
        obj['data'] = data;
    }

    /**
     * Constructs a <code>TransactionReceiptLog</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TransactionReceiptLog} obj Optional instance to populate.
     * @return {module:model/TransactionReceiptLog} The populated <code>TransactionReceiptLog</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TransactionReceiptLog();

            if (data.hasOwnProperty('log_index')) {
                obj['log_index'] = ApiClient.convertToType(data['log_index'], 'Number');
            }
            if (data.hasOwnProperty('address')) {
                obj['address'] = ApiClient.convertToType(data['address'], 'String');
            }
            if (data.hasOwnProperty('topics')) {
                obj['topics'] = ApiClient.convertToType(data['topics'], ['String']);
            }
            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], 'String');
            }
            if (data.hasOwnProperty('block_number')) {
                obj['block_number'] = ApiClient.convertToType(data['block_number'], 'Number');
            }
            if (data.hasOwnProperty('block_hash')) {
                obj['block_hash'] = ApiClient.convertToType(data['block_hash'], 'String');
            }
            if (data.hasOwnProperty('transaction_hash')) {
                obj['transaction_hash'] = ApiClient.convertToType(data['transaction_hash'], 'String');
            }
            if (data.hasOwnProperty('transaction_index')) {
                obj['transaction_index'] = ApiClient.convertToType(data['transaction_index'], 'Number');
            }
            if (data.hasOwnProperty('removed')) {
                obj['removed'] = ApiClient.convertToType(data['removed'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TransactionReceiptLog</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TransactionReceiptLog</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TransactionReceiptLog.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['address'] && !(typeof data['address'] === 'string' || data['address'] instanceof String)) {
            throw new Error("Expected the field `address` to be a primitive type in the JSON string but got " + data['address']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['topics'])) {
            throw new Error("Expected the field `topics` to be an array in the JSON data but got " + data['topics']);
        }
        // ensure the json data is a string
        if (data['data'] && !(typeof data['data'] === 'string' || data['data'] instanceof String)) {
            throw new Error("Expected the field `data` to be a primitive type in the JSON string but got " + data['data']);
        }
        // ensure the json data is a string
        if (data['block_hash'] && !(typeof data['block_hash'] === 'string' || data['block_hash'] instanceof String)) {
            throw new Error("Expected the field `block_hash` to be a primitive type in the JSON string but got " + data['block_hash']);
        }
        // ensure the json data is a string
        if (data['transaction_hash'] && !(typeof data['transaction_hash'] === 'string' || data['transaction_hash'] instanceof String)) {
            throw new Error("Expected the field `transaction_hash` to be a primitive type in the JSON string but got " + data['transaction_hash']);
        }

        return true;
    }


}

TransactionReceiptLog.RequiredProperties = ["log_index", "address", "topics", "data"];

/**
 * The index position of the log within the block.
 * @member {Number} log_index
 */
TransactionReceiptLog.prototype['log_index'] = undefined;

/**
 * The address of the contract that emitted the log.
 * @member {String} address
 */
TransactionReceiptLog.prototype['address'] = undefined;

/**
 * The indexed log arguments. The first topic is the hash of the event signature, and the remaining topics are the indexed event parameters, with a maximum of three.
 * @member {Array.<String>} topics
 */
TransactionReceiptLog.prototype['topics'] = undefined;

/**
 * The non-indexed log arguments, encoded as a hexadecimal string.
 * @member {String} data
 */
TransactionReceiptLog.prototype['data'] = undefined;

/**
 * The number of the block that contains the log.
 * @member {Number} block_number
 */
TransactionReceiptLog.prototype['block_number'] = undefined;

/**
 * The hash of the block that contains the log.
 * @member {String} block_hash
 */
TransactionReceiptLog.prototype['block_hash'] = undefined;

/**
 * The hash of the transaction that emitted the log.
 * @member {String} transaction_hash
 */
TransactionReceiptLog.prototype['transaction_hash'] = undefined;

/**
 * The index position within the block of the transaction that emitted the log.
 * @member {Number} transaction_index
 */
TransactionReceiptLog.prototype['transaction_index'] = undefined;

/**
 * Whether the log was removed due to a chain reorganization. - `true`: The log was removed because the block that contains it was reorganized out of the canonical chain. - `false`: The log is still valid. 
 * @member {Boolean} removed
 */
TransactionReceiptLog.prototype['removed'] = undefined;






export default TransactionReceiptLog;

