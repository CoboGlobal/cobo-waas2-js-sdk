/**
 * Cobo Wallet as a Service 2.0
 * Cobo WaaS 2.0 enables you to programmatically access Cobo's full suite of crypto wallet technologies with powerful and flexible access controls.  # Wallet technologies - Custodial Wallet - MPC Wallet - Smart Contract Wallet (Based on Safe{Wallet}) - Exchange Wallet  # Risk Control technologies - Workflow - Access Control List (ACL)  # Risk Control targets - Wallet Management   - User/team and their permission management   - Risk control configurations, e.g. whitelist, blacklist, rate-limiting etc. - Blockchain Interaction   - Crypto transfer   - Smart Contract Invocation  # Important HTTPS only. RESTful, resource oriented  # Get Started Set up your APIs or get authorization  # Authentication and Authorization CoboAuth  # Request and Response application/json  # Error Handling  ### Common error codes | Error Code | Description | | -- | -- |  ### API-specific error codes For error codes that are dedicated to a specific API, see the Error codes section in each API specification, for example, /v3/wallets.  # Rate and Usage Limiting  # Idempotent Request  # Pagination # Support [Developer Hub](https://cobo.com/developers) 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The MaxSendValue model module.
 * @module model/MaxSendValue
 * @version 0.1.0
 */
class MaxSendValue {
    /**
     * Constructs a new <code>MaxSendValue</code>.
     * max send value and fee.
     * @alias module:model/MaxSendValue
     */
    constructor() { 
        
        MaxSendValue.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>MaxSendValue</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MaxSendValue} obj Optional instance to populate.
     * @return {module:model/MaxSendValue} The populated <code>MaxSendValue</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MaxSendValue();

            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
            if (data.hasOwnProperty('token_decimal')) {
                obj['token_decimal'] = ApiClient.convertToType(data['token_decimal'], 'Number');
            }
            if (data.hasOwnProperty('fee_token')) {
                obj['fee_token'] = ApiClient.convertToType(data['fee_token'], 'String');
            }
            if (data.hasOwnProperty('fee_decimal')) {
                obj['fee_decimal'] = ApiClient.convertToType(data['fee_decimal'], 'Number');
            }
            if (data.hasOwnProperty('max_send_value')) {
                obj['max_send_value'] = ApiClient.convertToType(data['max_send_value'], 'String');
            }
            if (data.hasOwnProperty('fee_per_byte')) {
                obj['fee_per_byte'] = ApiClient.convertToType(data['fee_per_byte'], 'String');
            }
            if (data.hasOwnProperty('fee_value')) {
                obj['fee_value'] = ApiClient.convertToType(data['fee_value'], 'String');
            }
            if (data.hasOwnProperty('gas_price')) {
                obj['gas_price'] = ApiClient.convertToType(data['gas_price'], 'String');
            }
            if (data.hasOwnProperty('gas_limit')) {
                obj['gas_limit'] = ApiClient.convertToType(data['gas_limit'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MaxSendValue</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MaxSendValue</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }
        // ensure the json data is a string
        if (data['fee_token'] && !(typeof data['fee_token'] === 'string' || data['fee_token'] instanceof String)) {
            throw new Error("Expected the field `fee_token` to be a primitive type in the JSON string but got " + data['fee_token']);
        }
        // ensure the json data is a string
        if (data['max_send_value'] && !(typeof data['max_send_value'] === 'string' || data['max_send_value'] instanceof String)) {
            throw new Error("Expected the field `max_send_value` to be a primitive type in the JSON string but got " + data['max_send_value']);
        }
        // ensure the json data is a string
        if (data['fee_per_byte'] && !(typeof data['fee_per_byte'] === 'string' || data['fee_per_byte'] instanceof String)) {
            throw new Error("Expected the field `fee_per_byte` to be a primitive type in the JSON string but got " + data['fee_per_byte']);
        }
        // ensure the json data is a string
        if (data['fee_value'] && !(typeof data['fee_value'] === 'string' || data['fee_value'] instanceof String)) {
            throw new Error("Expected the field `fee_value` to be a primitive type in the JSON string but got " + data['fee_value']);
        }
        // ensure the json data is a string
        if (data['gas_price'] && !(typeof data['gas_price'] === 'string' || data['gas_price'] instanceof String)) {
            throw new Error("Expected the field `gas_price` to be a primitive type in the JSON string but got " + data['gas_price']);
        }

        return true;
    }


}



/**
 * token name.
 * @member {String} token
 */
MaxSendValue.prototype['token'] = undefined;

/**
 * coin decimal precision.
 * @member {Number} token_decimal
 */
MaxSendValue.prototype['token_decimal'] = undefined;

/**
 * fee token name.
 * @member {String} fee_token
 */
MaxSendValue.prototype['fee_token'] = undefined;

/**
 * fee coin decimal precision.
 * @member {Number} fee_decimal
 */
MaxSendValue.prototype['fee_decimal'] = undefined;

/**
 * the maximum sendable value for the given address or current wallet.
 * @member {String} max_send_value
 */
MaxSendValue.prototype['max_send_value'] = undefined;

/**
 * transaction fees per byte for UTXO model.
 * @member {String} fee_per_byte
 */
MaxSendValue.prototype['fee_per_byte'] = undefined;

/**
 * transaction fee for UTXO model
 * @member {String} fee_value
 */
MaxSendValue.prototype['fee_value'] = undefined;

/**
 * gas price for account model
 * @member {String} gas_price
 */
MaxSendValue.prototype['gas_price'] = undefined;

/**
 * gas limit for account model
 * @member {Number} gas_limit
 */
MaxSendValue.prototype['gas_limit'] = undefined;






export default MaxSendValue;

