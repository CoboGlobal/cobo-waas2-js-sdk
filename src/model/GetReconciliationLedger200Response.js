/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ReconLedgerEntry from './ReconLedgerEntry';

/**
 * The GetReconciliationLedger200Response model module.
 * @module model/GetReconciliationLedger200Response
 */
class GetReconciliationLedger200Response {
    /**
     * Constructs a new <code>GetReconciliationLedger200Response</code>.
     * @alias module:model/GetReconciliationLedger200Response
     * @param data {Array.<module:model/ReconLedgerEntry>} The list of reconciliation ledger entries. A single transaction may produce multiple entries (for example, an internal transfer), which share the same `transaction_id`.
     */
    constructor(data) { 
        
        GetReconciliationLedger200Response.initialize(this, data);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, data) { 
        obj['data'] = data;
    }

    /**
     * Constructs a <code>GetReconciliationLedger200Response</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetReconciliationLedger200Response} obj Optional instance to populate.
     * @return {module:model/GetReconciliationLedger200Response} The populated <code>GetReconciliationLedger200Response</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetReconciliationLedger200Response();

            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], [ReconLedgerEntry]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetReconciliationLedger200Response</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetReconciliationLedger200Response</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetReconciliationLedger200Response.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['data']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['data'])) {
                throw new Error("Expected the field `data` to be an array in the JSON data but got " + data['data']);
            }
            // validate the optional field `data` (array)
            for (const item of data['data']) {
                ReconLedgerEntry.validateJSON(item);
            };
        }

        return true;
    }


}

GetReconciliationLedger200Response.RequiredProperties = ["data"];

/**
 * The list of reconciliation ledger entries. A single transaction may produce multiple entries (for example, an internal transfer), which share the same `transaction_id`.
 * @member {Array.<module:model/ReconLedgerEntry>} data
 */
GetReconciliationLedger200Response.prototype['data'] = undefined;






export default GetReconciliationLedger200Response;

