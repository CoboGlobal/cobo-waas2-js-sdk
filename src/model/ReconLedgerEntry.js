/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ReconLedgerEntry model module.
 * @module model/ReconLedgerEntry
 */
class ReconLedgerEntry {
    /**
     * Constructs a new <code>ReconLedgerEntry</code>.
     * The reconciliation ledger entry, representing an address&#39;s running balance after a transaction.
     * @alias module:model/ReconLedgerEntry
     * @param transaction_id {String} The transaction ID (the Cobo transaction ID you provided in `transaction_ids`).
     * @param wallet_id {String} The wallet ID.
     * @param address {String} The wallet address involved in this entry.
     * @param token_id {String} The token ID, which is the unique identifier of a token.
     * @param chain_id {String} The chain ID, which is the unique identifier of a blockchain.
     * @param amount {String} The transaction amount for this entry, expressed in the token's main unit (already divided by the token's decimals). The value is signed - positive for deposits and negative for withdrawals.
     * @param balance_after {String} The running balance of the address for this token after this transaction, expressed in the token's main unit.
     */
    constructor(transaction_id, wallet_id, address, token_id, chain_id, amount, balance_after) { 
        
        ReconLedgerEntry.initialize(this, transaction_id, wallet_id, address, token_id, chain_id, amount, balance_after);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, transaction_id, wallet_id, address, token_id, chain_id, amount, balance_after) { 
        obj['transaction_id'] = transaction_id;
        obj['wallet_id'] = wallet_id;
        obj['address'] = address;
        obj['token_id'] = token_id;
        obj['chain_id'] = chain_id;
        obj['amount'] = amount;
        obj['balance_after'] = balance_after;
    }

    /**
     * Constructs a <code>ReconLedgerEntry</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReconLedgerEntry} obj Optional instance to populate.
     * @return {module:model/ReconLedgerEntry} The populated <code>ReconLedgerEntry</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReconLedgerEntry();

            if (data.hasOwnProperty('transaction_id')) {
                obj['transaction_id'] = ApiClient.convertToType(data['transaction_id'], 'String');
            }
            if (data.hasOwnProperty('block_time')) {
                obj['block_time'] = ApiClient.convertToType(data['block_time'], 'Number');
            }
            if (data.hasOwnProperty('wallet_id')) {
                obj['wallet_id'] = ApiClient.convertToType(data['wallet_id'], 'String');
            }
            if (data.hasOwnProperty('address')) {
                obj['address'] = ApiClient.convertToType(data['address'], 'String');
            }
            if (data.hasOwnProperty('token_id')) {
                obj['token_id'] = ApiClient.convertToType(data['token_id'], 'String');
            }
            if (data.hasOwnProperty('chain_id')) {
                obj['chain_id'] = ApiClient.convertToType(data['chain_id'], 'String');
            }
            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'String');
            }
            if (data.hasOwnProperty('balance_after')) {
                obj['balance_after'] = ApiClient.convertToType(data['balance_after'], 'String');
            }
            if (data.hasOwnProperty('transaction_hash')) {
                obj['transaction_hash'] = ApiClient.convertToType(data['transaction_hash'], 'String');
            }
            if (data.hasOwnProperty('block_number')) {
                obj['block_number'] = ApiClient.convertToType(data['block_number'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReconLedgerEntry</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReconLedgerEntry</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ReconLedgerEntry.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['transaction_id'] && !(typeof data['transaction_id'] === 'string' || data['transaction_id'] instanceof String)) {
            throw new Error("Expected the field `transaction_id` to be a primitive type in the JSON string but got " + data['transaction_id']);
        }
        // ensure the json data is a string
        if (data['wallet_id'] && !(typeof data['wallet_id'] === 'string' || data['wallet_id'] instanceof String)) {
            throw new Error("Expected the field `wallet_id` to be a primitive type in the JSON string but got " + data['wallet_id']);
        }
        // ensure the json data is a string
        if (data['address'] && !(typeof data['address'] === 'string' || data['address'] instanceof String)) {
            throw new Error("Expected the field `address` to be a primitive type in the JSON string but got " + data['address']);
        }
        // ensure the json data is a string
        if (data['token_id'] && !(typeof data['token_id'] === 'string' || data['token_id'] instanceof String)) {
            throw new Error("Expected the field `token_id` to be a primitive type in the JSON string but got " + data['token_id']);
        }
        // ensure the json data is a string
        if (data['chain_id'] && !(typeof data['chain_id'] === 'string' || data['chain_id'] instanceof String)) {
            throw new Error("Expected the field `chain_id` to be a primitive type in the JSON string but got " + data['chain_id']);
        }
        // ensure the json data is a string
        if (data['amount'] && !(typeof data['amount'] === 'string' || data['amount'] instanceof String)) {
            throw new Error("Expected the field `amount` to be a primitive type in the JSON string but got " + data['amount']);
        }
        // ensure the json data is a string
        if (data['balance_after'] && !(typeof data['balance_after'] === 'string' || data['balance_after'] instanceof String)) {
            throw new Error("Expected the field `balance_after` to be a primitive type in the JSON string but got " + data['balance_after']);
        }
        // ensure the json data is a string
        if (data['transaction_hash'] && !(typeof data['transaction_hash'] === 'string' || data['transaction_hash'] instanceof String)) {
            throw new Error("Expected the field `transaction_hash` to be a primitive type in the JSON string but got " + data['transaction_hash']);
        }

        return true;
    }


}

ReconLedgerEntry.RequiredProperties = ["transaction_id", "wallet_id", "address", "token_id", "chain_id", "amount", "balance_after"];

/**
 * The transaction ID (the Cobo transaction ID you provided in `transaction_ids`).
 * @member {String} transaction_id
 */
ReconLedgerEntry.prototype['transaction_id'] = undefined;

/**
 * The time when the block containing the transaction was created, in Unix timestamp format, measured in milliseconds.
 * @member {Number} block_time
 */
ReconLedgerEntry.prototype['block_time'] = undefined;

/**
 * The wallet ID.
 * @member {String} wallet_id
 */
ReconLedgerEntry.prototype['wallet_id'] = undefined;

/**
 * The wallet address involved in this entry.
 * @member {String} address
 */
ReconLedgerEntry.prototype['address'] = undefined;

/**
 * The token ID, which is the unique identifier of a token.
 * @member {String} token_id
 */
ReconLedgerEntry.prototype['token_id'] = undefined;

/**
 * The chain ID, which is the unique identifier of a blockchain.
 * @member {String} chain_id
 */
ReconLedgerEntry.prototype['chain_id'] = undefined;

/**
 * The transaction amount for this entry, expressed in the token's main unit (already divided by the token's decimals). The value is signed - positive for deposits and negative for withdrawals.
 * @member {String} amount
 */
ReconLedgerEntry.prototype['amount'] = undefined;

/**
 * The running balance of the address for this token after this transaction, expressed in the token's main unit.
 * @member {String} balance_after
 */
ReconLedgerEntry.prototype['balance_after'] = undefined;

/**
 * The transaction hash on the blockchain.
 * @member {String} transaction_hash
 */
ReconLedgerEntry.prototype['transaction_hash'] = undefined;

/**
 * The number of the block containing the transaction.
 * @member {Number} block_number
 */
ReconLedgerEntry.prototype['block_number'] = undefined;






export default ReconLedgerEntry;

