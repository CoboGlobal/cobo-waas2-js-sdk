/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TokenizationERC20FundTokenPermissionParams model module.
 * @module model/TokenizationERC20FundTokenPermissionParams
 */
class TokenizationERC20FundTokenPermissionParams {
    /**
     * Constructs a new <code>TokenizationERC20FundTokenPermissionParams</code>.
     * Role-based permission settings for ERC-4626 fund token contract. If not provided, all permissions will be granted to the issuance wallet by default.
     * @alias module:model/TokenizationERC20FundTokenPermissionParams
     */
    constructor() { 
        
        TokenizationERC20FundTokenPermissionParams.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>TokenizationERC20FundTokenPermissionParams</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TokenizationERC20FundTokenPermissionParams} obj Optional instance to populate.
     * @return {module:model/TokenizationERC20FundTokenPermissionParams} The populated <code>TokenizationERC20FundTokenPermissionParams</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TokenizationERC20FundTokenPermissionParams();

            if (data.hasOwnProperty('owner')) {
                obj['owner'] = ApiClient.convertToType(data['owner'], ['String']);
            }
            if (data.hasOwnProperty('manager')) {
                obj['manager'] = ApiClient.convertToType(data['manager'], ['String']);
            }
            if (data.hasOwnProperty('nav_updater')) {
                obj['nav_updater'] = ApiClient.convertToType(data['nav_updater'], ['String']);
            }
            if (data.hasOwnProperty('redemption_approver')) {
                obj['redemption_approver'] = ApiClient.convertToType(data['redemption_approver'], ['String']);
            }
            if (data.hasOwnProperty('settlement_operator')) {
                obj['settlement_operator'] = ApiClient.convertToType(data['settlement_operator'], ['String']);
            }
            if (data.hasOwnProperty('emergency_guardian')) {
                obj['emergency_guardian'] = ApiClient.convertToType(data['emergency_guardian'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TokenizationERC20FundTokenPermissionParams</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TokenizationERC20FundTokenPermissionParams</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['owner'])) {
            throw new Error("Expected the field `owner` to be an array in the JSON data but got " + data['owner']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['manager'])) {
            throw new Error("Expected the field `manager` to be an array in the JSON data but got " + data['manager']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['nav_updater'])) {
            throw new Error("Expected the field `nav_updater` to be an array in the JSON data but got " + data['nav_updater']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['redemption_approver'])) {
            throw new Error("Expected the field `redemption_approver` to be an array in the JSON data but got " + data['redemption_approver']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['settlement_operator'])) {
            throw new Error("Expected the field `settlement_operator` to be an array in the JSON data but got " + data['settlement_operator']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['emergency_guardian'])) {
            throw new Error("Expected the field `emergency_guardian` to be an array in the JSON data but got " + data['emergency_guardian']);
        }

        return true;
    }


}



/**
 * List of addresses for the owner/admin role. Have full administrative control over the fund contract.
 * @member {Array.<String>} owner
 */
TokenizationERC20FundTokenPermissionParams.prototype['owner'] = undefined;

/**
 * List of addresses for the fund manager role. Can manage fund configurations and operational parameters.
 * @member {Array.<String>} manager
 */
TokenizationERC20FundTokenPermissionParams.prototype['manager'] = undefined;

/**
 * List of addresses for the NAV updater role. Can update the net asset value (NAV) per share price.
 * @member {Array.<String>} nav_updater
 */
TokenizationERC20FundTokenPermissionParams.prototype['nav_updater'] = undefined;

/**
 * List of addresses for the redemption approver role. Can approve or reject investor redemption requests.
 * @member {Array.<String>} redemption_approver
 */
TokenizationERC20FundTokenPermissionParams.prototype['redemption_approver'] = undefined;

/**
 * List of addresses for the settlement operator role. Can execute investment and redemption settlement operations.
 * @member {Array.<String>} settlement_operator
 */
TokenizationERC20FundTokenPermissionParams.prototype['settlement_operator'] = undefined;

/**
 * List of addresses for the emergency guardian role. Can trigger emergency actions such as pausing the fund.
 * @member {Array.<String>} emergency_guardian
 */
TokenizationERC20FundTokenPermissionParams.prototype['emergency_guardian'] = undefined;






export default TokenizationERC20FundTokenPermissionParams;

