/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PaymentBankAccountBalance model module.
 * @module model/PaymentBankAccountBalance
 */
class PaymentBankAccountBalance {
    /**
     * Constructs a new <code>PaymentBankAccountBalance</code>.
     * @alias module:model/PaymentBankAccountBalance
     * @param bank_account_id {String} The destination bank account ID.
     * @param currency {String} The fiat currency of the bank account.
     * @param total_balance {String} The total balance of the bank account.
     * @param available_balance {String} The available balance of the bank account.
     * @param locked_balance {String} The locked balance of the bank account.
     */
    constructor(bank_account_id, currency, total_balance, available_balance, locked_balance) { 
        
        PaymentBankAccountBalance.initialize(this, bank_account_id, currency, total_balance, available_balance, locked_balance);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, bank_account_id, currency, total_balance, available_balance, locked_balance) { 
        obj['bank_account_id'] = bank_account_id;
        obj['currency'] = currency;
        obj['total_balance'] = total_balance;
        obj['available_balance'] = available_balance;
        obj['locked_balance'] = locked_balance;
    }

    /**
     * Constructs a <code>PaymentBankAccountBalance</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PaymentBankAccountBalance} obj Optional instance to populate.
     * @return {module:model/PaymentBankAccountBalance} The populated <code>PaymentBankAccountBalance</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaymentBankAccountBalance();

            if (data.hasOwnProperty('bank_account_id')) {
                obj['bank_account_id'] = ApiClient.convertToType(data['bank_account_id'], 'String');
            }
            if (data.hasOwnProperty('currency')) {
                obj['currency'] = ApiClient.convertToType(data['currency'], 'String');
            }
            if (data.hasOwnProperty('total_balance')) {
                obj['total_balance'] = ApiClient.convertToType(data['total_balance'], 'String');
            }
            if (data.hasOwnProperty('available_balance')) {
                obj['available_balance'] = ApiClient.convertToType(data['available_balance'], 'String');
            }
            if (data.hasOwnProperty('locked_balance')) {
                obj['locked_balance'] = ApiClient.convertToType(data['locked_balance'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaymentBankAccountBalance</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaymentBankAccountBalance</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PaymentBankAccountBalance.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['bank_account_id'] && !(typeof data['bank_account_id'] === 'string' || data['bank_account_id'] instanceof String)) {
            throw new Error("Expected the field `bank_account_id` to be a primitive type in the JSON string but got " + data['bank_account_id']);
        }
        // ensure the json data is a string
        if (data['currency'] && !(typeof data['currency'] === 'string' || data['currency'] instanceof String)) {
            throw new Error("Expected the field `currency` to be a primitive type in the JSON string but got " + data['currency']);
        }
        // ensure the json data is a string
        if (data['total_balance'] && !(typeof data['total_balance'] === 'string' || data['total_balance'] instanceof String)) {
            throw new Error("Expected the field `total_balance` to be a primitive type in the JSON string but got " + data['total_balance']);
        }
        // ensure the json data is a string
        if (data['available_balance'] && !(typeof data['available_balance'] === 'string' || data['available_balance'] instanceof String)) {
            throw new Error("Expected the field `available_balance` to be a primitive type in the JSON string but got " + data['available_balance']);
        }
        // ensure the json data is a string
        if (data['locked_balance'] && !(typeof data['locked_balance'] === 'string' || data['locked_balance'] instanceof String)) {
            throw new Error("Expected the field `locked_balance` to be a primitive type in the JSON string but got " + data['locked_balance']);
        }

        return true;
    }


}

PaymentBankAccountBalance.RequiredProperties = ["bank_account_id", "currency", "total_balance", "available_balance", "locked_balance"];

/**
 * The destination bank account ID.
 * @member {String} bank_account_id
 */
PaymentBankAccountBalance.prototype['bank_account_id'] = undefined;

/**
 * The fiat currency of the bank account.
 * @member {String} currency
 */
PaymentBankAccountBalance.prototype['currency'] = undefined;

/**
 * The total balance of the bank account.
 * @member {String} total_balance
 */
PaymentBankAccountBalance.prototype['total_balance'] = undefined;

/**
 * The available balance of the bank account.
 * @member {String} available_balance
 */
PaymentBankAccountBalance.prototype['available_balance'] = undefined;

/**
 * The locked balance of the bank account.
 * @member {String} locked_balance
 */
PaymentBankAccountBalance.prototype['locked_balance'] = undefined;






export default PaymentBankAccountBalance;

