/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TransactionExtraType from './TransactionExtraType';

/**
 * The TransactionFeePayer model module.
 * @module model/TransactionFeePayer
 */
class TransactionFeePayer {
    /**
     * Constructs a new <code>TransactionFeePayer</code>.
     * The extra information of fee payer.
     * @alias module:model/TransactionFeePayer
     * @param extra_type {module:model/TransactionExtraType} 
     */
    constructor(extra_type) { 
        
        TransactionFeePayer.initialize(this, extra_type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, extra_type) { 
        obj['extra_type'] = extra_type;
    }

    /**
     * Constructs a <code>TransactionFeePayer</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TransactionFeePayer} obj Optional instance to populate.
     * @return {module:model/TransactionFeePayer} The populated <code>TransactionFeePayer</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TransactionFeePayer();

            if (data.hasOwnProperty('extra_type')) {
                obj['extra_type'] = TransactionExtraType.constructFromObject(data['extra_type']);
            }
            if (data.hasOwnProperty('fee_payer')) {
                obj['fee_payer'] = ApiClient.convertToType(data['fee_payer'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TransactionFeePayer</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TransactionFeePayer</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TransactionFeePayer.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['fee_payer'] && !(typeof data['fee_payer'] === 'string' || data['fee_payer'] instanceof String)) {
            throw new Error("Expected the field `fee_payer` to be a primitive type in the JSON string but got " + data['fee_payer']);
        }

        return true;
    }


}

TransactionFeePayer.RequiredProperties = ["extra_type"];

/**
 * @member {module:model/TransactionExtraType} extra_type
 */
TransactionFeePayer.prototype['extra_type'] = undefined;

/**
 * The address of the designated Solana fee payer account that covers the transaction fees, separating the fee payment from the main signer or source account.
 * @member {String} fee_payer
 */
TransactionFeePayer.prototype['fee_payer'] = undefined;






export default TransactionFeePayer;

