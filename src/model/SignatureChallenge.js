/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SignatureChallenge model module.
 * @module model/SignatureChallenge
 */
class SignatureChallenge {
    /**
     * Constructs a new <code>SignatureChallenge</code>.
     * A signature challenge for self-custody wallet verification. The wallet owner must sign the &#x60;challenge&#x60; string with the wallet associated with &#x60;address&#x60; and submit the signature via [Submit Travel Rule information for deposits](#operation/submit_deposit_travel_rule_info) or [withdrawals](#operation/submit_withdraw_travel_rule_info). The challenge is time-bounded — each call rotates the challenge and only the most recently-issued value will verify. 
     * @alias module:model/SignatureChallenge
     * @param address {String} The self-custody wallet address that must sign this challenge. This is the counterparty address for the transaction: for deposits it is the sender's wallet, and for withdrawals it is the recipient's wallet. 
     * @param challenge {String} The human-readable, time-sensitive message to sign. Contains the wallet address, a unique nonce, and a timestamp. 
     * @param expires_in {Number} Number of seconds before the challenge expires. The signature must be submitted within this window.
     */
    constructor(address, challenge, expires_in) { 
        
        SignatureChallenge.initialize(this, address, challenge, expires_in);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, address, challenge, expires_in) { 
        obj['address'] = address;
        obj['challenge'] = challenge;
        obj['expires_in'] = expires_in;
    }

    /**
     * Constructs a <code>SignatureChallenge</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SignatureChallenge} obj Optional instance to populate.
     * @return {module:model/SignatureChallenge} The populated <code>SignatureChallenge</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SignatureChallenge();

            if (data.hasOwnProperty('address')) {
                obj['address'] = ApiClient.convertToType(data['address'], 'String');
            }
            if (data.hasOwnProperty('challenge')) {
                obj['challenge'] = ApiClient.convertToType(data['challenge'], 'String');
            }
            if (data.hasOwnProperty('expires_in')) {
                obj['expires_in'] = ApiClient.convertToType(data['expires_in'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SignatureChallenge</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SignatureChallenge</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SignatureChallenge.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['address'] && !(typeof data['address'] === 'string' || data['address'] instanceof String)) {
            throw new Error("Expected the field `address` to be a primitive type in the JSON string but got " + data['address']);
        }
        // ensure the json data is a string
        if (data['challenge'] && !(typeof data['challenge'] === 'string' || data['challenge'] instanceof String)) {
            throw new Error("Expected the field `challenge` to be a primitive type in the JSON string but got " + data['challenge']);
        }

        return true;
    }


}

SignatureChallenge.RequiredProperties = ["address", "challenge", "expires_in"];

/**
 * The self-custody wallet address that must sign this challenge. This is the counterparty address for the transaction: for deposits it is the sender's wallet, and for withdrawals it is the recipient's wallet. 
 * @member {String} address
 */
SignatureChallenge.prototype['address'] = undefined;

/**
 * The human-readable, time-sensitive message to sign. Contains the wallet address, a unique nonce, and a timestamp. 
 * @member {String} challenge
 */
SignatureChallenge.prototype['challenge'] = undefined;

/**
 * Number of seconds before the challenge expires. The signature must be submitted within this window.
 * @member {Number} expires_in
 */
SignatureChallenge.prototype['expires_in'] = undefined;






export default SignatureChallenge;

