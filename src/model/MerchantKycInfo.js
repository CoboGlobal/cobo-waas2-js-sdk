/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import MerchantKycStatus from './MerchantKycStatus';

/**
 * The MerchantKycInfo model module.
 * @module model/MerchantKycInfo
 */
class MerchantKycInfo {
    /**
     * Constructs a new <code>MerchantKycInfo</code>.
     * @alias module:model/MerchantKycInfo
     * @param kyc_submission_id {String} The KYC submission ID.
     * @param merchant_id {String} The merchant ID.
     * @param status {module:model/MerchantKycStatus} 
     */
    constructor(kyc_submission_id, merchant_id, status) { 
        
        MerchantKycInfo.initialize(this, kyc_submission_id, merchant_id, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, kyc_submission_id, merchant_id, status) { 
        obj['kyc_submission_id'] = kyc_submission_id;
        obj['merchant_id'] = merchant_id;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>MerchantKycInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MerchantKycInfo} obj Optional instance to populate.
     * @return {module:model/MerchantKycInfo} The populated <code>MerchantKycInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MerchantKycInfo();

            if (data.hasOwnProperty('kyc_submission_id')) {
                obj['kyc_submission_id'] = ApiClient.convertToType(data['kyc_submission_id'], 'String');
            }
            if (data.hasOwnProperty('merchant_id')) {
                obj['merchant_id'] = ApiClient.convertToType(data['merchant_id'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = MerchantKycStatus.constructFromObject(data['status']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MerchantKycInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MerchantKycInfo</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MerchantKycInfo.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['kyc_submission_id'] && !(typeof data['kyc_submission_id'] === 'string' || data['kyc_submission_id'] instanceof String)) {
            throw new Error("Expected the field `kyc_submission_id` to be a primitive type in the JSON string but got " + data['kyc_submission_id']);
        }
        // ensure the json data is a string
        if (data['merchant_id'] && !(typeof data['merchant_id'] === 'string' || data['merchant_id'] instanceof String)) {
            throw new Error("Expected the field `merchant_id` to be a primitive type in the JSON string but got " + data['merchant_id']);
        }

        return true;
    }


}

MerchantKycInfo.RequiredProperties = ["kyc_submission_id", "merchant_id", "status"];

/**
 * The KYC submission ID.
 * @member {String} kyc_submission_id
 */
MerchantKycInfo.prototype['kyc_submission_id'] = undefined;

/**
 * The merchant ID.
 * @member {String} merchant_id
 */
MerchantKycInfo.prototype['merchant_id'] = undefined;

/**
 * @member {module:model/MerchantKycStatus} status
 */
MerchantKycInfo.prototype['status'] = undefined;






export default MerchantKycInfo;

