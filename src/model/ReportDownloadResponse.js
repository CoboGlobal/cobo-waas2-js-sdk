/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ReportDownloadResponse model module.
 * @module model/ReportDownloadResponse
 */
class ReportDownloadResponse {
    /**
     * Constructs a new <code>ReportDownloadResponse</code>.
     * @alias module:model/ReportDownloadResponse
     * @param download_url {String} The temporary URL used to download the payment report.
     */
    constructor(download_url) { 
        
        ReportDownloadResponse.initialize(this, download_url);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, download_url) { 
        obj['download_url'] = download_url;
    }

    /**
     * Constructs a <code>ReportDownloadResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReportDownloadResponse} obj Optional instance to populate.
     * @return {module:model/ReportDownloadResponse} The populated <code>ReportDownloadResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReportDownloadResponse();

            if (data.hasOwnProperty('download_url')) {
                obj['download_url'] = ApiClient.convertToType(data['download_url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReportDownloadResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReportDownloadResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ReportDownloadResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['download_url'] && !(typeof data['download_url'] === 'string' || data['download_url'] instanceof String)) {
            throw new Error("Expected the field `download_url` to be a primitive type in the JSON string but got " + data['download_url']);
        }

        return true;
    }


}

ReportDownloadResponse.RequiredProperties = ["download_url"];

/**
 * The temporary URL used to download the payment report.
 * @member {String} download_url
 */
ReportDownloadResponse.prototype['download_url'] = undefined;






export default ReportDownloadResponse;

