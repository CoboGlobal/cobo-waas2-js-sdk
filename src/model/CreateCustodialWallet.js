/**
 * Cobo Wallet as a Service 2.0
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BaseCreateWallet from './BaseCreateWallet';

/**
 * The CreateCustodialWallet model module.
 * @module model/CreateCustodialWallet
 * @version 0.1.0
 */
class CreateCustodialWallet {
    /**
     * Constructs a new <code>CreateCustodialWallet</code>.
     * @alias module:model/CreateCustodialWallet
     * @implements module:model/BaseCreateWallet
     * @param name {String} 
     * @param walletType {module:model/CreateCustodialWallet.WalletTypeEnum} 
     * @param walletSubtype {module:model/CreateCustodialWallet.WalletSubtypeEnum} 
     */
    constructor(name, walletType, walletSubtype) { 
        BaseCreateWallet.initialize(this, name);
        CreateCustodialWallet.initialize(this, name, walletType, walletSubtype);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, walletType, walletSubtype) { 
        obj['name'] = name;
        obj['wallet_type'] = walletType;
        obj['wallet_subtype'] = walletSubtype;
    }

    /**
     * Constructs a <code>CreateCustodialWallet</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateCustodialWallet} obj Optional instance to populate.
     * @return {module:model/CreateCustodialWallet} The populated <code>CreateCustodialWallet</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateCustodialWallet();
            BaseCreateWallet.constructFromObject(data, obj);

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('wallet_type')) {
                obj['wallet_type'] = ApiClient.convertToType(data['wallet_type'], 'String');
            }
            if (data.hasOwnProperty('wallet_subtype')) {
                obj['wallet_subtype'] = ApiClient.convertToType(data['wallet_subtype'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateCustodialWallet</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateCustodialWallet</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateCustodialWallet.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['wallet_type'] && !(typeof data['wallet_type'] === 'string' || data['wallet_type'] instanceof String)) {
            throw new Error("Expected the field `wallet_type` to be a primitive type in the JSON string but got " + data['wallet_type']);
        }
        // ensure the json data is a string
        if (data['wallet_subtype'] && !(typeof data['wallet_subtype'] === 'string' || data['wallet_subtype'] instanceof String)) {
            throw new Error("Expected the field `wallet_subtype` to be a primitive type in the JSON string but got " + data['wallet_subtype']);
        }

        return true;
    }


}

CreateCustodialWallet.RequiredProperties = ["name", "wallet_type", "wallet_subtype"];

/**
 * @member {String} name
 */
CreateCustodialWallet.prototype['name'] = undefined;

/**
 * @member {module:model/CreateCustodialWallet.WalletTypeEnum} wallet_type
 */
CreateCustodialWallet.prototype['wallet_type'] = undefined;

/**
 * @member {module:model/CreateCustodialWallet.WalletSubtypeEnum} wallet_subtype
 */
CreateCustodialWallet.prototype['wallet_subtype'] = undefined;


// Implement BaseCreateWallet interface:
/**
 * @member {String} name
 */
BaseCreateWallet.prototype['name'] = undefined;



/**
 * Allowed values for the <code>wallet_type</code> property.
 * @enum {String}
 * @readonly
 */
CreateCustodialWallet['WalletTypeEnum'] = {

    /**
     * value: "Custodial"
     * @const
     */
    "Custodial": "Custodial"
};


/**
 * Allowed values for the <code>wallet_subtype</code> property.
 * @enum {String}
 * @readonly
 */
CreateCustodialWallet['WalletSubtypeEnum'] = {

    /**
     * value: "Asset"
     * @const
     */
    "Asset": "Asset",

    /**
     * value: "Web3"
     * @const
     */
    "Web3": "Web3"
};



export default CreateCustodialWallet;

